
CI Python em: push: branches: - main pull_request: branches: - main jobs: test: runs-on: ubuntu-latest steps: - nome: Fazer checkout do código uses: actions/checkout@v2 - nome: Configurar o Python uses: actions/setup-python@v2 with: python-version: '3.8' - nome: Instalar dependências run: | python -m pip install --upgrade pip pip install -r requirements.txt - nome: Executar testes run: | pytest
